context CompilationUnit {
	$parallel self.imports.size() > 2
	constraint allImportsAreUsed {
		check: self.imports.forAll(imp |
			NamedElement.allInstances().exists(ne | ne.originalCompilationUnit == self)
		)
	}
}